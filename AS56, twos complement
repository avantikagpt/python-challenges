binary = input("Enter the 8 bit binary number")
while len(binary) != 8:
    binary = input("Enter the 8 bit binary number")

den1 = int(binary,2)
den2 = den1*(-1)

binary2 = bin(den2)

answer = input("Enter your two's complement answer")

print ("The correct answer is", binary2)
